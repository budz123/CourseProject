<Window x:Class="TermPaper.MVVM.View.ReservationsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TermPaper.MVVM.View"
        mc:Ignorable="d"
        Title="ReservationsView" Height="450" Width="800"
        xmlns:viewmodel="clr-namespace:TermPaper.MVVM.ViewModel">
    <Window.DataContext>
        <viewmodel:ReservationsViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="0,4*"/>
        </Grid.RowDefinitions>
        <Button Content="Delete" HorizontalAlignment="Left" Height="57" Margin="66,0,0,0" VerticalAlignment="Center" Width="137" Command="{Binding ReservationsDeleteViewCommand}"/>
        <Button Content="Edit" HorizontalAlignment="Left" Height="51" Margin="320,15,0,0" VerticalAlignment="Top" Width="138 " Command="{Binding ReservationsEditViewViewCommand}"/>
        <Button Content="Add" HorizontalAlignment="Left" Height="51" Margin="575,0,0,0" VerticalAlignment="Center" Width="132" Command="{Binding ReservationsAddViewCommand}"/>
        <Border Grid.Row="1">
            <ContentControl  Content="{Binding CurrentView}">

            </ContentControl>
        </Border>
    </Grid>
</Window>
