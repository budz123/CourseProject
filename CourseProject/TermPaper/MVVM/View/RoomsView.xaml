<Window x:Class="TermPaper.MVVM.View.RoomsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TermPaper.MVVM.View"
        mc:Ignorable="d"
        Title="RoomsView" Height="470" Width="800"
        xmlns:viewmodel="clr-namespace:TermPaper.MVVM.ViewModel">
    <Window.DataContext>
        <viewmodel:RoomsViewModel/>
    </Window.DataContext>
    <Grid Margin="0,0,0,7">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="0,2*"/>
        </Grid.RowDefinitions>
        <Button Content="ButtonDelete" HorizontalAlignment="Left" Height="50" Margin="53,28,0,0" VerticalAlignment="Top" Width="103" Command="{Binding RoomsDeleteViewCommand}"/>
        <Button Content="ButtonEdit" HorizontalAlignment="Left" Height="50" Margin="300,28,0,0" VerticalAlignment="Top" Width="140" Command="{Binding RoomsEditViewViewCommand}"/>
        <Button Content="Buttonadd" HorizontalAlignment="Left" Height="50" Margin="595,28,0,0" VerticalAlignment="Top" Width="137" Command="{Binding RoomsAddViewCommand}"/>
        <Border Grid.Row="1">
            <ContentControl  Content="{Binding CurrentView}">

            </ContentControl>
        </Border>
      
    </Grid>
</Window>
